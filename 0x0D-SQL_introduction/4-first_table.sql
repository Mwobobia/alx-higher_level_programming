-- Creates a table called first_table with values values id INT and name VARCHAR(256) in the current database in my MySQL server.

CREATE TABLE IF NOT EXISTS first_table(
    id INT,
    name VARCHAR(256)
);

